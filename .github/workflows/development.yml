name: development

on:
  push:
    branches: [ "dev" ]

jobs:
  info:
    runs-on: ubuntu-latest

    steps:
      - name: checkout code
        uses: actions/checkout@v3

      - name: show architecture
        run: make info

  test:
    runs-on: ubuntu-latest

    steps:
      - name: checkout code
        uses: actions/checkout@v3

      - name: build test
        run: make test

      - name: run test
        run: echo "gas\n" | ./bin/test davidgaspardev
