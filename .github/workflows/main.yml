name: d-string

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  test:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: build test
      run: make test

    - name: run test
      run: ./bin/test davidgaspardev
      
