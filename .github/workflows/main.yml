name: d-string

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  info:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: show architecture
        run: make info

  test:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: build test
      run: make test

    - name: run test
      run: echo "gas\n" | ./bin/test davidgaspardev
